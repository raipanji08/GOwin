{
  "name": "gowin-midtrans-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check src/index.js && node --check src/firebase.js && node --check src/midtrans.js",
    "test": "node --test",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^4.115.0"
  }
}
